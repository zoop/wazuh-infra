# Pin the Wazuh dashboard to the dev-stage-spot-3 GKE node pool.

apiVersion: apps/v1
kind: Deployment
metadata:
  name: wazuh-dashboard
  namespace: wazuh
spec:
  template:
    spec:
      nodeSelector:
        cloud.google.com/gke-nodepool: dev-stage-spot-3
