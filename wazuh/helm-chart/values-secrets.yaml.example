# Copy to values-secrets.yaml (gitignored) and fill in real values.
# Deploy with:
#   helm upgrade --install wazuh . -f values-production.yaml -f values-secrets.yaml
secrets:
  indexerPassword: "change_me_strong_password"
  apiPassword: "change_me_strong_password"

iris:
  enabled: false
  apiKey: "get_from_iris_ui_after_first_boot"
