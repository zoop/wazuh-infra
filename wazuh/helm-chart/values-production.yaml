manager:
  resources:
    limits:
      cpu: "2000m"
      memory: "4Gi"
    requests:
      cpu: "1000m"
      memory: "2Gi"
  storage:
    size: 100Gi
    storageClass: "standard"

indexer:
  resources:
    limits:
      cpu: "2000m"
      memory: "4Gi"
    requests:
      cpu: "1000m"
      memory: "2Gi"
  storage:
    size: 100Gi
    storageClass: "standard"
  javaOpts: "-Xms2g -Xmx2g"

dashboard:
  resources:
    limits:
      cpu: "1000m"
      memory: "2Gi"
    requests:
      cpu: "500m"
      memory: "1Gi"

agentService:
  type: LoadBalancer

wazuh:
  alertLevel: 3
